services:
  prism:
    image: stoplight/prism:5
    container_name: library-prism
    ports:
      - "4010:4010"
    volumes:
      - ./openapi/library-api.yaml:/tmp/library-api.yaml:ro
    command: ["mock", "/tmp/library-api.yaml", "-h", "0.0.0.0", "-p", "4010", "--multiprocess=false"]
